name: EnsureTorch
description: Converts `numpy.ndarray` to `torch.Tensor`, pipes `torch.Tensor` through
cite: []
authors:
    - Fynn Beuttenmüller
documentation: transformations.md
tags: [torch, pytorch, numpy, ndarray, to, tensor]
license: MIT

format_version: 0.1.0
language: python
framework: pytorch

dependencies: ../../env_pytorch.yaml
source: pybio.torch.transformations.generic.EnsureTorch
optional_kwargs: {apply_to: null}

inputs: any
outputs: same
