name: NormalizeZeroMeanUnitVariance
description: Normalize input to have zero mean, unit variance.
cite:
    - text: TODO
      doi: TODO
authors:
    - Constantin Pape
documentation: normalize_zero_mean_unit_varaince.md
tags: [activation, sigmoid]

format_version: 0.1.0
language: python
framework: pytorch

dependencies: ../../env_no_pytorch.yaml

source: torchbio.transformations.normalizations.NormalizeZeroMeanUnitVariance
required_kwargs: [apply_to]
default_kwargs: [eps: 1.0e-6, mean: null, std: null]

inputs: any
outputs: same
